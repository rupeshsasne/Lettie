# Checked-in defaults live in keystore.properties (same values).
# Paths are relative to the project root.

storeFile=androidApp/keystore/lettie-release.jks
storePassword=LettieRelease2026!
keyAlias=lettie
keyPassword=LettieRelease2026!
